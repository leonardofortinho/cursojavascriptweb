//As palavras reservadas são: var(variável), let(variável) e const(constante)

var a = 3 //var de declaração de variável. No var você tem quem declarar a variável duas vezes

/*let regra geral de criação de variável. Aqui você só precisa declarar uma vez e depois alterar.
dessa forma você declara uma vez como let e depois só precisa mexer na variável.
*/
let b = 4 

var a = 30
b = 40

console.log(a, b)

const c = 5 //Não tem como alterar a constante (segue exemplo)
// c = 50
console.log(c)